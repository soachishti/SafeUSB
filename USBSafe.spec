# -*- mode: python -*-
a = Analysis(['USBSafe.py'],
             pathex=['C:\\Users\\soac\\Desktop\\PythonTool\\USBSafe'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='USBSafe.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='USBSafe')
